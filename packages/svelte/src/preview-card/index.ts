// PreviewCard is a read-only floating preview; shares all Popover parts.
export {
  PopoverRoot as PreviewCardRoot,
  PopoverTrigger as PreviewCardTrigger,
  PopoverPositioner as PreviewCardPositioner,
  PopoverPopup as PreviewCardPopup,
  PopoverArrow as PreviewCardArrow,
} from '../popover/index.js';
